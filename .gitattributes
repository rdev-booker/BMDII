# Normalize line endings to LF in the repo, CRLF on Windows checkout
* text=auto eol=lf

# Force binary for images — never mangle them
*.jpg  binary
*.jpeg binary
*.png  binary
*.webp binary
*.ico  binary
*.svg  binary
